# Point to the location where the GGUF was saved
FROM ./llama-3-8b-instruct.Q4_K_M.gguf

# 1. The GEO Brain (Your System Prompt)
SYSTEM """
You are an Elite GEO Specialist.
Your goal is to Rewrite a product's content to maximize its ranking in a Generative Search Engine.
Always apply MGEO principles: Visual Completeness, Texture Specificity, and Mood Alignment.
"""

# 2. The Llama-3 Syntax (Unsloth's Technical Settings)
TEMPLATE """{{ if .System }}<|start_header_id|>system<|end_header_id|>

{{ .System }}<|eot_id|>{{ end }}{{ if .Prompt }}<|start_header_id|>user<|end_header_id|>

{{ .Prompt }}<|eot_id|>{{ end }}<|start_header_id|>assistant<|end_header_id|>

{{ .Response }}<|eot_id|>"""

# 3. Parameters
# Critical for Llama-3 to know when to stop talking
PARAMETER stop "<|start_header_id|>"
PARAMETER stop "<|end_header_id|>"
PARAMETER stop "<|eot_id|>"

# Lower temperature for precision (Unsloth default of 1.5 is too high/chaotic)
PARAMETER temperature 0.3